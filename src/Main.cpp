#include <cstring>
#include <iostream>
#include <cstdlib>
#include <ctime>
#include "Graph.h"

using namespace std;

int main() {
	Graph* test = new Graph();
	test->srcGraphInit();
	// test->runTest();
	// cout << test->entry->getNext()->getNext()->getNext()->getLabel() << endl;
	// printNodes(test->entry);
    return 0;
}